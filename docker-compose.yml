metamascara:
  build: ./
  restart: always
  ports:
    - "9001"
  environment:
    MASCARA_ORIGIN: "https://wallet.moac.io"
    VIRTUAL_PORT: "9001"
    VIRTUAL_HOST: "wallet.moac.io"
    LETSENCRYPT_HOST: "wallet.moac.io"
    LETSENCRYPT_EMAIL: "admin@moac.io"